1

Maksymalna liczba recept to 3 w pliku

testAddRecipe linia 76 dodanie 4 recepty powinno zwróciæ false

test to testAddRecipe()

2
usuniêty Recpie pozostaje w Tablicy recipeFull jako true; i nie kasujemy elementu

usuniêcie Recepie w klasie CoffeMaker.java metoda public boolean deleteRecipe(Recipe r) 

test to testDeleteRecipe() linia 99

3

addInventory w CoffeMaker.java linia 150 nie pozwala na dodanie cukru - nie akceptuje wartoœci powy¿ej 0

test to testAddInventory()

4

makeCoffe nie zmienia odpowiedniej liczby coffe w inventory 
by³o inventory.setCoffee(inventory.getCoffee() + r.getAmtCoffee()); powinno byæ inventory.setCoffee(inventory.getCoffee() - r.getAmtCoffee());x

test to MakeCoffe() 


5

editRecipe pozwala³ na zmianê nowego recpie z inna nazwa

w public boolean editRecipe(Recipe oldRecipe, Recipe newRecipe) zamienione  if(newRecipe.equals(recipeArray[i])) { na if(oldRecipe.equals(recipeArray[i])) {

edit recipe po dodaniu maksymalnej iloœci Recipe edit siê nie powodzi poniewa¿ nie zwalniamy miejsca po edytowanym Recipe

test to testEditRecipe()